# PrarieML landing page

This is a static website generated through `pelican`

## Building

The site depends on `pelican`:

```
pip install pelican
make publish
```

## Development builds

```
make devserver
```
The site is now served locally on `localhost:8000`
